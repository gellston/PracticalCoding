<Window x:Class="MVVMBasic.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMBasic"
        xmlns:view="clr-namespace:MVVMBasic.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=MainWindowViewModel}">

    <Window.Resources>

    </Window.Resources>
    <DockPanel>
        <!--<DockPanel.Resources>
            <Style TargetType="{x:Type Button}" x:Key="TopMenuNewButton">
                <Setter Property="Background" Value="Green"></Setter>
                <Setter Property="Content" Value="New"></Setter>
            </Style>
        </DockPanel.Resources>-->
        <DockPanel Style="{StaticResource TopMenuPanel}">
            <Button Style="{StaticResource TopMenuNewButton}"></Button>
            <Button Style="{StaticResource TopMenuButton}" Content="Open"></Button>
            <Button Style="{StaticResource TopMenuButton}" Content="Copy"></Button>
            <Border></Border>
        </DockPanel>
        <Border></Border>

    </DockPanel>
</Window>
